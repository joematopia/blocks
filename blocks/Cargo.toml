[package]
name = "blocks"
version = "0.1.0"
edition = "2024"

[dependencies]
md5 = "0.7.0"

[profile.release]
strip = true      # Removes all debug symbols (anonymizes the code)
panic = "abort"   # Prevents stack-trace leakage on crashes
